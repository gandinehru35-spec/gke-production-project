apiVersion: v1
kind: Service
metadata:
  name: hello-app-svc
  namespace: prod
spec:
  type: ClusterIP # Internal service only
  selector:
    app: hello-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
